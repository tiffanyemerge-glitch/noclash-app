  </main>

  <footer class="note">
    <% if (typeof demoNote !== 'undefined' && demoNote) { %><%- demoNote %><% } else { %>
      Prototype build &mdash; payments and outbound email are stubbed. See README.md.
    <% } %>
  </footer>
</div>
</body>
</html>
